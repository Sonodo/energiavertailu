# Fingrid Open Data API key — register at https://data.fingrid.fi/en/instructions
# Used by /api/energy/production and /api/energy/household-benchmark.
# Without a key, those endpoints return sample/degraded responses.
FINGRID_API_KEY=
